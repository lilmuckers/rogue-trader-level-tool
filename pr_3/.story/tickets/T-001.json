{
  "blockedBy": [],
  "completedDate": null,
  "createdDate": "2026-05-07",
  "description": "Wrap the existing Store abstraction so all keys are prefixed with rt.p.{profileId}. Global keys (UUID, profiles list) use raw Store. Should be transparent to callers — swap Store → ProfileStore(id) and all reads/writes automatically scope to that profile.",
  "id": "T-001",
  "order": 10,
  "phase": "multi-profile",
  "status": "open",
  "title": "ProfileStore: namespace all localStorage keys per profile",
  "type": "task"
}
